// Objeto Math

let resultado;
resultado = Math.PI;
resultado = Math.round(2.6); // redondea hasta el más cercano. 5 hacia abajo
resultado = Math.ceil(2.2); // redondea hacia arriba
resultado = Math.floor(2.9); // redondea hacia abajo 
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-200); // convierte a positivo
resultado = Math.min(3, 5, 22, 4, 5, 6, 1); // número menor
resultado = Math.max(3, 5, 22, 4, 5, 6, 1); // número mayor
resultado = Math.random(); // aleatorio
resultado = Math.floor(Math.random() * 10); // aleatorio del 0 al número que se ponga



console.log(resultado)
